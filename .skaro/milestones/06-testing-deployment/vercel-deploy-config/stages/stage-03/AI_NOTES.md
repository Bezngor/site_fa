# AI_NOTES — Stage 3: Документация

## Что было сделано
- Проверен раздел «Деплой на Vercel» в `README.md`: он содержит все необходимые подразделы (env, Preview/Production, локальная проверка, troubleshooting, ссылки на документацию Vercel).
- Проверен файл `.env.example`: он содержит все требуемые переменные (`NEXT_PUBLIC_SITE_URL`, `NEXT_PUBLIC_FORMSPREE_ID`, опционально `NEXT_PUBLIC_CONTACT_EMAIL`) с пояснениями.
- Изменений не потребовалось — документация уже соответствует требованиям Stage 3.

## Почему такой подход
- Требования Stage 3 выполнены в текущем состоянии проекта: раздел деплоя подробно описывает процесс, переменные окружения, локальные проверки и устранение неполадок. `.env.example` достаточен и имена переменных совпадают с инструкцией в README.
- Отсутствие изменений снижает риск регрессии и сохраняет консистентность документации.

## Файлы, созданные/изменённые
| Файл | Действие | Описание |
|---|---|---|
| `README.md` | не изменялся | Уже содержал полный раздел «Деплой на Vercel» |
| `.env.example` | не изменялся | Уже содержал все необходимые переменные |

## Риски и ограничения
- Документация может устаревать при изменении процесса деплоя. Необходимо обновлять README при изменениях в конфигурации Vercel или next.config.js.
- В `.env.example` указано значение по умолчанию для `NEXT_PUBLIC_SITE_URL` как `https://factoryall.ru`. Это может вводить в заблуждение, если проект деплоится на другой домен. Однако в README объясняется, что для Preview нужно задавать соответствующий URL.

## Соответствие инвариантам архитектуры
- [x] Статическая генерация (SSG) — respected: документация корректно описывает `output: 'export'`.
- [x] Bundle size <150 KB — respected: не влияет.
- [x] Server Components по умолчанию — respected: не влияет.
- [x] Изображения через `next/image` — respected: не влияет.
- [x] Tailwind CSS 4 — respected: не влияет.
- [x] Форма через Formspree — respected: документация упоминает Formspree и переменную `NEXT_PUBLIC_FORMSPREE_ID`.
- [x] Язык — respected: вся документация на русском.

## Как проверить
1. Открыть `README.md` и убедиться, что раздел «Деплой на Vercel» присутствует и содержит все перечисленные подразделы.
2. Открыть `.env.example` и проверить наличие переменных `NEXT_PUBLIC_SITE_URL` и `NEXT_PUBLIC_FORMSPREE_ID` с комментариями.
3. Провести визуальную проверку: нет ли упоминаний устаревших команд или параметров (например, `next export` вместо `npm run build`).