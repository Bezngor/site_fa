# AI_NOTES — Этап 1: Создание системы типов

## Что было сделано
- Создана директория `types/`.
- Создан файл `types/index.ts`, содержащий все необходимые интерфейсы для статических данных лендинга.
- Реализованы интерфейсы: `Metric`, `Case`, `HowWeWorkStep`, `Product`, `HeroTexts`, `SectionTexts`, `TextsData`.

## Почему выбран такой подход
- Использование интерфейсов обеспечивает строгую типизацию для всех данных, которые будут импортироваться в компоненты.
- Структура данных соответствует требованиям FR-01–FR-06 (плоский массив продуктов, строгая схема метрик, массив шагов для HowWeWork).
- Разделение на мелкие интерфейсы позволяет переиспользовать их в разных частях приложения.

## Файлы созданы / изменены
| Файл | Действие | Описание |
|---|---|---|
| `types/index.ts` | Создан | Определение типов данных для всего проекта |

## Риски и ограничения
- Рисков нет, так как типы являются декларативными и не влияют на runtime-производительность.

## Соответствие инвариантам
- [x] Использование TypeScript — соблюдено.
- [x] Отсутствие заглушек — соблюдено.
- [x] Соответствие спецификации данных — соблюдено.

## Как проверить
1. Выполнить команду `npx tsc --noEmit` в корне проекта для проверки корректности типов.