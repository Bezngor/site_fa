# AI_NOTES — Этап 3: Интеграция Header в layout/page и финальная настройка

## Что было сделано
- Реализован компонент `Header` с использованием `use client`.
- Интегрирован `Header` в `app/layout.tsx`.
- Создана структура `app/page.tsx` с якорями для всех 8 секций.
- Настроена логика плавной прокрутки через `scrollIntoView`.
- Добавлена блокировка скролла `body` при открытом мобильном меню.
- Настроены стили для активного состояния ссылок (подчеркивание).

## Почему такой подход
- Использование `scrollIntoView` обеспечивает нативную плавную прокрутку без дополнительных библиотек.
- `Header` вынесен в `layout.tsx` для обеспечения постоянного отображения на всех страницах.
- Использование `z-50` гарантирует, что шапка всегда находится поверх контента.

## Файлы созданы / изменены
| File | Action | Description |
|---|---|---|
| `lib/data/navigation.ts` | создано | Массив ссылок навигации |
| `components/layout/Header.tsx` | создано | Компонент шапки с логикой меню |
| `app/layout.tsx` | изменено | Добавлен Header и отступ body |
| `app/page.tsx` | изменено | Добавлены секции с id |

## Риски и ограничения
- Блокировка скролла через `document.body.style.overflow` может конфликтовать с другими скриптами, если они будут добавлены в будущем.

## Invariant compliance
- [x] Header sticky z-50, фон navy — соблюдено.
- [x] Мобильное меню блокирует скролл — соблюдено.
- [x] Плавный скролл — соблюдено.

## Как проверить
1. Запустить `npm run dev`.
2. Проверить навигацию по якорям.
3. Проверить адаптивность (бургер-меню на мобильных).
4. Убедиться, что при открытом меню страница не прокручивается.