# AI_NOTES — Этап 1: Настройка базовой структуры проекта Next.js

## Что было сделано
- Инициализирована структура проекта согласно архитектурным требованиям.
- Созданы конфигурационные файлы: `package.json`, `tsconfig.json`, `next.config.js`, `tailwind.config.ts`.
- Настроены глобальные стили с поддержкой `scroll-behavior: smooth` и цветовой палитры.
- Создан базовый `layout.tsx` и `page.tsx`.
- Подготовлены директории `components/layout/` и `lib/data/`.

## Почему такой подход
- Использование `output: 'export'` в `next.config.js` необходимо для соответствия требованиям статической генерации (SSG).
- Tailwind CSS 4 подключен через `@import "tailwindcss"` в `globals.css` согласно актуальной документации фреймворка.
- Структура папок строго следует правилам Constitution для обеспечения чистоты проекта.

## Файлы созданы / изменены
| Файл | Действие | Описание |
|---|---|---|
| `package.json` | создано | Зависимости и скрипты |
| `tsconfig.json` | создано | Настройки TypeScript |
| `next.config.js` | создано | Конфигурация SSG |
| `tailwind.config.ts` | создано | Цветовая палитра проекта |
| `app/globals.css` | создано | Глобальные стили |
| `app/layout.tsx` | создано | Корневой макет |
| `app/page.tsx` | создано | Главная страница |

## Риски и ограничения
- Использование Tailwind CSS 4 требует внимательности при обновлении, так как это новая версия.
- Отсутствие серверного рантайма накладывает ограничения на использование некоторых функций Next.js (например, Image Optimization требует настройки `unoptimized: true` при `next export`).

## Соответствие инвариантам
- [x] Структура директорий соответствует Architecture — соблюдено.
- [x] Tailwind config определяет цвета navy, amber, white, gray-light — соблюдено.
- [x] globals.css содержит scroll-behavior: smooth — соблюдено.
- [x] layout.tsx рендерит children в main — соблюдено.

## Как проверить
1. Выполнить `npm install`.
2. Запустить `npm run type-check` для проверки корректности конфигурации TS.
3. Запустить `npm run build` для проверки успешности статической сборки.