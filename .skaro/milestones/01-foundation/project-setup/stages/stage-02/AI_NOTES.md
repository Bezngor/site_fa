# AI_NOTES — Этап 2: Настройка стилизации Tailwind

## Что было сделано
- Обновлен `tailwind.config.ts`: добавлены цвета `primary` (#1B2A4A) и `accent` (#F59E0B) в секцию `extend`.
- Обновлен `app/globals.css`: настроено поведение `scroll-behavior: smooth` и базовые стили для `body` с использованием новых цветов.
- Создан файл `.prettierrc` с конфигурацией для автоматической сортировки классов Tailwind.
- Обновлен `package.json`: добавлен `prettier-plugin-tailwindcss` для обеспечения консистентности стилей.

## Почему выбран этот подход
- Использование `extend` в конфигурации Tailwind позволяет сохранить стандартные утилиты фреймворка, добавляя при этом специфичные для бренда цвета.
- `prettier-plugin-tailwindcss` гарантирует, что классы в HTML/JSX всегда отсортированы в правильном порядке, что упрощает поддержку кода.

## Файлы созданные / измененные
| Файл | Действие | Описание |
|---|---|---|
| `tailwind.config.ts` | изменено | Добавлена палитра проекта. |
| `app/globals.css` | изменено | Настроены базовые стили и плавный скролл. |
| `.prettierrc` | создано | Настройка форматирования и сортировки Tailwind. |
| `package.json` | изменено | Добавлен плагин для Prettier. |

## Риски и ограничения
- Использование Tailwind CSS 4 требует корректной настройки `@import "tailwindcss";` в CSS файлах.
- Необходимо убедиться, что все разработчики используют Prettier с установленным плагином.

## Соответствие инвариантам
- [x] Цветовая палитра primary/accent — соблюдено.
- [x] Tailwind CSS 4 — соблюдено.
- [x] Соответствие стандартам — соблюдено.

## Как проверить
1. Выполнить `npm install` для установки плагина Prettier.
2. Проверить применение цветов, используя классы `bg-primary` или `text-accent` в компонентах.
3. Запустить `npx prettier --check .` для подтверждения отсутствия ошибок форматирования.