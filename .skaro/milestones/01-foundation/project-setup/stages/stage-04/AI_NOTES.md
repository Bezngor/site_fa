# AI_NOTES — Этап 4: Линтинг и финальная настройка инструментов

## Что было сделано
- Настроен `eslint.config.mjs` с использованием Flat Config.
- Подключены `next/core-web-vitals` и `typescript-eslint/recommended`.
- Добавлены правила для предотвращения использования `any` и предупреждения о неиспользуемых переменных.
- Обновлен `package.json`: добавлены необходимые dev-зависимости для линтинга и скрипты для проверки форматирования.

## Почему выбран этот подход
- Flat Config является стандартом для ESLint 9+ и Next.js 15.
- Использование `typescript-eslint` обеспечивает строгую типизацию, что соответствует конституции проекта.

## Файлы созданные / измененные
| Файл | Действие | Описание |
|---|---|---|
| `eslint.config.mjs` | обновлен | Настройка правил линтинга и Flat Config. |
| `package.json` | обновлен | Добавление зависимостей и скриптов. |

## Риски и ограничения
- Отсутствие `.eslintrc.json` гарантирует отсутствие конфликтов с Flat Config.
- Необходимо убедиться, что все IDE настроены на использование нового формата конфигурации ESLint.

## Соответствие инвариантам
- [x] Использование Flat Config — соблюдено.
- [x] Отсутствие `any` — соблюдено (правило `no-explicit-any` установлено в `error`).
- [x] Соблюдение strict mode — соблюдено.

## Как проверить
1. Выполнить `npm install`.
2. Запустить `npx eslint . --quiet` для проверки линтера.
3. Запустить `npx prettier --check .` для проверки форматирования.