# AI_NOTES — Финализация конфигурации проекта

## Что было сделано
- Проведена проверка всех конфигурационных файлов (`package.json`, `tailwind.config.ts`, `eslint.config.mjs`, `next.config.js`).
- Добавлен файл `.prettierrc` с плагином `prettier-plugin-tailwindcss` для автоматической сортировки классов Tailwind.
- Убедился, что все зависимости соответствуют требованиям Next.js 15 и архитектурным решениям.

## Почему выбран этот подход
- Использование `prettier-plugin-tailwindcss` обеспечивает единообразие стилей во всех компонентах, что критично для поддержки проекта.
- Настройки соответствуют конституции проекта (strict mode, отсутствие `any`, SSG).

## Файлы созданные / измененные
| Файл | Действие | Описание |
|---|---|---|
| `package.json` | проверен | Актуализация скриптов и зависимостей. |
| `.prettierrc` | создан | Настройка форматирования и сортировки Tailwind. |

## Риски и ограничения
- Отсутствуют. Проект готов к разработке контентных секций.

## Соответствие инвариантам
- [x] Next.js 15 + App Router — соблюдено.
- [x] Tailwind CSS 4 — соблюдено.
- [x] Статическая генерация (SSG) — соблюдено.
- [x] Строгая типизация (strict: true) — соблюдено.

## Как проверить
1. Выполнить `npm install`.
2. Выполнить `npm run build` для проверки корректности статического экспорта.
3. Выполнить `npm run format:check` для проверки форматирования кода.