# AI_NOTES — Этап 1: Конфигурация зависимостей и сборки

## Что было сделано
- Обновлен `package.json`: добавлены зависимости `zod` и `lucide-react`.
- Обновлен `tsconfig.json`: параметр `strict` установлен в `true` для обеспечения строгой типизации.
- Обновлен `next.config.js`: установлены параметры `output: 'export'` для статической генерации и `images: { unoptimized: true }` для совместимости с `next/image` в режиме статического экспорта.

## Почему выбран этот подход
- Использование `zod` необходимо для валидации контактной формы согласно требованиям безопасности.
- `lucide-react` выбран как стандартная библиотека иконок для экосистемы Next.js.
- Параметры сборки соответствуют требованиям архитектуры (ADR-001, ADR-004) для обеспечения работы на Vercel как статического сайта.

## Созданные / измененные файлы
| Файл | Действие | Описание |
|---|---|---|
| `package.json` | изменено | Добавлены зависимости, сохранены версии Next.js/React. |
| `tsconfig.json` | изменено | Включен строгий режим типизации. |
| `next.config.js` | изменено | Настроена статическая генерация. |

## Риски и ограничения
- Версии Next.js и React оставлены без изменений, чтобы избежать конфликтов с существующей кодовой базой.
- Режим `unoptimized: true` для изображений является обязательным компромиссом для статического экспорта `next/image`.

## Соответствие инвариантам
- [x] Строгая типизация (`strict: true`) — соблюдено.
- [x] Статическая генерация (`next export`) — соблюдено.
- [x] Отсутствие серверного рантайма — соблюдено.

## Как проверить
1. Выполнить `npm install` для установки новых зависимостей.
2. Выполнить `npx tsc --noEmit` для проверки отсутствия ошибок типизации.
3. Выполнить `npm run build` для подтверждения успешной генерации папки `out/`.