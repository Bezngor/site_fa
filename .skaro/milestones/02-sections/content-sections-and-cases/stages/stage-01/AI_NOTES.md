# AI_NOTES — Этап 1: Подготовка данных

## Что было сделано
- Обновлен файл `types/index.ts`: добавлены интерфейсы `CaseDetail` и `ProductCategory`, обновлен интерфейс `Case`.
- Создан файл `lib/data/cases.ts`: содержит массив из 3 кейсов с необходимой структурой (Задача, Решение, Результат).
- Создан файл `lib/data/products.ts`: содержит простой массив категорий продуктов.
- Обновлен файл `lib/data/texts.ts`: добавлены недостающие объекты для секций Hero, Problems, Results, About, при этом секция HowWeWork оставлена без изменений.

## Почему выбран такой подход
- Использование типизированных статических файлов обеспечивает строгую проверку типов TypeScript и упрощает поддержку контента.
- Разделение данных по файлам (`cases.ts`, `products.ts`, `texts.ts`) соответствует принципу единственной ответственности.

## Файлы созданы / изменены
| Файл | Действие | Описание |
|---|---|---|
| `types/index.ts` | изменено | Добавлены интерфейсы для данных |
| `lib/data/cases.ts` | создано | Данные кейсов с детализацией |
| `lib/data/products.ts` | создано | Список категорий продуктов |
| `lib/data/texts.ts` | изменено | Добавлены тексты для секций |

## Риски и ограничения
- Рисков нет, так как данные являются статическими и не требуют серверной логики.

## Соответствие инвариантам
- [x] Использование TypeScript — соблюдено.
- [x] Отсутствие БД — соблюдено.
- [x] Соответствие цветовой палитре — соблюдено (тексты не содержат хардкода цветов).

## Как проверить
1. Выполнить `npx tsc --noEmit` для проверки корректности типов.
2. Проверить содержимое файлов на соответствие структуре из спецификации.