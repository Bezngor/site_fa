# AI_NOTES — Этап 2: Реализация ProblemsSection

## Что было сделано
- Создан компонент `ProblemsSection.tsx` в `components/sections/`.
- Реализована сетка с использованием CSS Grid (`grid-cols-1 md:grid-cols-2`).
- Добавлена стилизация согласно палитре: основной текст `#1B2A4A`, акцентные номера `#F59E0B`.
- Добавлена анимация `animate-fade-in` (предполагается наличие в `globals.css` или стандартном Tailwind).
- Обновлен `components/sections/index.ts` для экспорта нового компонента.
- Обновлен `app/page.tsx` для отображения секции после `HeroSection`.

## Почему этот подход
- Использование `grid` обеспечивает адаптивность без лишних медиа-запросов в JS.
- Компонент является Server Component, что соответствует требованиям производительности и архитектуре проекта.
- Данные импортируются из централизованного файла `texts.ts`, что гарантирует типизацию.

## Файлы созданы / изменены
| Файл | Действие | Описание |
|---|---|---|
| `components/sections/ProblemsSection.tsx` | создан | Основной компонент секции проблем |
| `components/sections/index.ts` | изменен | Добавлен экспорт компонента |
| `app/page.tsx` | изменен | Интеграция секции на главную страницу |

## Риски и ограничения
- Анимация `animate-fade-in` должна быть определена в `tailwind.config.ts` или `globals.css`. Если класс отсутствует, необходимо добавить его в `tailwind.config.ts` (keyframes).

## Соответствие инвариантам
- [x] Соблюдена строгая типизация.
- [x] Использованы только Server Components.
- [x] Соблюдена цветовая палитра проекта.
- [x] Соблюдены требования по адаптивности (1/2 колонки).

## Как проверить
1. Запустить `npm run dev` и открыть страницу.
2. Проверить отображение сетки на мобильных (ширина < 768px) и десктопных устройствах.
3. Убедиться, что номера имеют цвет `#F59E0B`.