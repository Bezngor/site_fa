# AI_NOTES — Этап 1: Подготовка данных для секции ProblemsSection

## Что было сделано
- Обновлен интерфейс `TextsData` в `types/index.ts`: поле `problems` теперь типизировано как `SectionTexts & { list: string[] }`.
- Обновлен файл `lib/data/texts.ts`: добавлена секция `problems` с заголовком, описанием и списком из 5 проблем.

## Почему этот подход
- Использование типизированного объекта `texts` обеспечивает строгую проверку типов во всем приложении.
- Структура данных соответствует спецификации (заголовок, описание, массив строк).

## Файлы созданы / изменены
| Файл | Действие | Описание |
|---|---|---|
| `types/index.ts` | изменено | Обновление интерфейса `TextsData` |
| `lib/data/texts.ts` | изменено | Добавление данных для `problems` |

## Риски и ограничения
- Рисков нет, так как изменения чисто структурные и типизированные.

## Соответствие инвариантам
- [x] Соблюдена строгая типизация (TypeScript).
- [x] Данные хранятся в статическом файле (SSG-friendly).
- [x] Использован русский язык для всех текстовых полей.

## Как проверить
1. Выполнить `npx tsc --noEmit` для проверки корректности типов.
2. Проверить наличие экспортируемого объекта `texts.problems` в `lib/data/texts.ts`.