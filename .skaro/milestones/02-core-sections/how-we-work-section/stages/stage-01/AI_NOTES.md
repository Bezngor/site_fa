# AI_NOTES — Этап 1: Подготовка статических данных

## Что было сделано
- Обновлен файл `lib/data/texts.ts`.
- В объект `howWeWork` добавлен массив `steps`, содержащий ровно 5 этапов (Аудит, Проектирование, Разработка, Внедрение, Поддержка) в соответствии с требованиями спецификации.
- Каждый шаг типизирован интерфейсом `HowWeWorkStep` из `types/index.ts`.

## Почему выбран этот подход
- Использование статического массива данных обеспечивает предсказуемость и простоту рендеринга в Server Component.
- Структура данных полностью соответствует требованиям FR-01 и Clarifications Q2.

## Созданные / измененные файлы
| Файл | Действие | Описание |
|---|---|---|
| `lib/data/texts.ts` | изменено | Добавлены данные для секции "Как мы работаем" |

## Риски и ограничения
- Рисков нет, так как структура данных соответствует существующим интерфейсам в `types/index.ts`.

## Соответствие инвариантам
- [x] Использование TypeScript — соблюдено.
- [x] Соблюдение структуры данных — соблюдено.
- [x] Отсутствие бизнес-логики в данных — соблюдено.

## Как проверить
1. Выполнить `npx tsc --noEmit` для проверки типизации.
2. Проверить наличие 5 объектов в `texts.howWeWork.steps` через `grep` или просмотр файла.