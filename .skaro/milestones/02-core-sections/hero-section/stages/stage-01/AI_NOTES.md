# AI_NOTES — Этап 1: Подготовка данных и глобальных стилей для HeroSection

## Что было сделано
- Обновлен интерфейс `HeroTexts` в `types/index.ts`, добавлено поле `ctaText`.
- Обновлен объект `texts` в `lib/data/texts.ts`, добавлено поле `ctaText` для секции `hero`.
- Убедился, что `app/globals.css` содержит `scroll-behavior: smooth` для корректной работы якорных ссылок.

## Почему выбран такой подход
- Использование централизованного хранилища данных (`lib/data/texts.ts`) соответствует требованиям архитектуры и обеспечивает типизацию через `types/index.ts`.
- Плавный скролл реализован через CSS, что является наиболее производительным и простым способом для статического лендинга.

## Файлы созданы / изменены
| Файл | Действие | Описание |
|---|---|---|
| `types/index.ts` | изменено | Добавление `ctaText` в `HeroTexts` |
| `lib/data/texts.ts` | изменено | Добавление данных для `hero` |
| `app/globals.css` | изменено | Убедился в наличии `scroll-behavior: smooth` |

## Риски и ограничения
- Рисков нет, изменения соответствуют спецификации и архитектурным принципам.

## Соответствие инвариантам
- [x] Использование TypeScript — соблюдено.
- [x] Статические данные в `lib/data` — соблюдено.
- [x] Отсутствие серверного рантайма — соблюдено.

## Как проверить
1. Проверить типы: `npx tsc --noEmit`.
2. Проверить наличие `scroll-behavior` в `app/globals.css`.
3. Проверить структуру `texts.hero` в `lib/data/texts.ts`.