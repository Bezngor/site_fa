# AI_NOTES — Этап 2: Реализация и интеграция HeroSection

## Что было сделано
- Создан компонент `HeroSection.tsx` в `components/sections/`.
- Реализован `next/image` с атрибутом `priority` для оптимизации LCP.
- Добавлен градиентный оверлей для обеспечения контрастности текста.
- Реализована якорная ссылка на секцию `#cta`.
- Интегрирован `HeroSection` в `app/page.tsx`.
- Создан файл `components/sections/index.ts` для экспорта секций.

## Почему выбран такой подход
- Использование `next/image` с `fill` и `object-cover` позволяет адаптивно заполнять экран.
- Использование `a` тега с `href="#cta"` обеспечивает нативную поддержку `scroll-behavior: smooth` без лишнего JS.
- Компонент является Server Component, что соответствует архитектуре SSG.

## Файлы созданы / изменены
| Файл | Действие | Описание |
|---|---|---|
| `components/sections/HeroSection.tsx` | создано | Основной компонент Hero |
| `components/sections/index.ts` | создано | Индексный файл для секций |
| `app/page.tsx` | изменено | Интеграция HeroSection |

## Риски и ограничения
- Изображение `/images/hero-bg.webp` должно быть добавлено в папку `public/images/` для корректной работы.

## Соответствие инвариантам
- [x] Использование TypeScript — соблюдено.
- [x] Server Components по умолчанию — соблюдено.
- [x] Tailwind CSS 4 — соблюдено.
- [x] Отсутствие серверного рантайма — соблюдено.

## Как проверить
1. Запустить `npm run build` для проверки сборки.
2. Проверить наличие `HeroSection` в `app/page.tsx`.
3. Убедиться, что `scroll-behavior: smooth` работает при клике на CTA.