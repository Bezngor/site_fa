/**
 * Якоря главной — синхронно с `lib/data/navigation.ts` (navLinks href без `#`).
 */
export const SECTION_IDS = [
  'hero',
  'problems',
  'how-we-work',
  'cases',
  'products',
  'results',
  'about',
  'contact',
] as const;
